import {useEffect,useRef,useId} from 'react';
import {Icon} from './ui';
export default function Modal({title,children,footer,onClose,small=false,busy=false}){
 const ref=useRef(null),id=useId(),closeRef=useRef(onClose),busyRef=useRef(busy);closeRef.current=onClose;busyRef.current=busy;
 useEffect(()=>{const prev=document.activeElement;const box=ref.current;const first=box.querySelector('input,select,textarea,button');first?.focus();const key=e=>{if(e.key==='Escape'){e.preventDefault();if(!busyRef.current)closeRef.current();}if(e.key==='Tab'){const all=[...box.querySelectorAll('button:not(:disabled),input:not(:disabled),select:not(:disabled),textarea:not(:disabled),a[href],[tabindex="0"]')].filter(n=>n.offsetParent!==null);if(!all.length){e.preventDefault();return;}const first=all[0],last=all.at(-1);if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus();}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus();}}};box.addEventListener('keydown',key);return()=>{box.removeEventListener('keydown',key);prev?.focus?.();};},[]);
 return <div className="sk-modal-backdrop" onMouseDown={e=>{if(e.target===e.currentTarget&&!busy)onClose();}}><section ref={ref} className={`sk-modal ${small?'small':''}`} role="dialog" aria-modal="true" aria-labelledby={id}><header className="sk-modal-header"><h2 id={id}>{title}</h2><button type="button" className="sk-icon-button" aria-label="Zamknij okno" onClick={onClose} disabled={busy}><Icon name="close"/></button></header><div className="sk-modal-body">{children}</div>{footer&&<footer className="sk-modal-footer">{footer}</footer>}</section></div>;
}
